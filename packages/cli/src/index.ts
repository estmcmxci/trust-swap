#!/usr/bin/env node
/**
 * @trust-swap/cli — Phase −1.B scaffold
 *
 * Real `tru swap` and `tru agent run` commands land in Phase 1 + Phase 4
 * per ../PLAN.md. For now this is a placeholder that confirms the
 * @trust-swap/core dep wiring works.
 */

import { SCAFFOLD_VERSION } from "@trust-swap/core";

console.log(`tru CLI — scaffold ${SCAFFOLD_VERSION}`);
console.log("Commands not yet implemented. See PLAN.md for the build sequence.");
