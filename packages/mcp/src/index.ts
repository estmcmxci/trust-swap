#!/usr/bin/env node
/**
 * @trust-swap/mcp — Phase −1.B scaffold
 *
 * Real MCP server exposing the trust_gated_swap tool lands in Phase 4
 * (or as a cut-line per PLAN.md). For now this is a placeholder.
 */

import { SCAFFOLD_VERSION } from "@trust-swap/core";

console.log(`trust-swap-mcp — scaffold ${SCAFFOLD_VERSION}`);
console.log("MCP server not yet implemented. See PLAN.md.");
